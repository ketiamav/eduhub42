import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Check } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PillarForm } from "@/components/forms";
import { pillars } from "@/lib/site-data";

export const Route = createFileRoute("/pillars")({ head: () => ({ meta: [{ title: "Our Pillars | EduHub42 Education Ecosystem" }, { name: "description", content: "Explore EduHub Learn, EduHub Enroll and EduHub Workforce—connected pathways for learning, education access and career readiness." }, { property: "og:title", content: "Our Pillars | EduHub42 Education Ecosystem" }, { property: "og:description", content: "One ecosystem. Multiple pathways for learners and students." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: "/pillars" }] }), component: PillarsPage });
function PillarsPage(){return <><section className="dark-band pt-44 pb-24"><div className="page-container"><p className="eyebrow">One ecosystem. Multiple pathways.</p><h1 className="section-title mt-4">Our Pillars</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-primary-foreground/75">Support for the classroom, the next application and the world of work—designed to move with every learner’s journey.</p></div></section><section className="section-pad"><div className="page-container space-y-16">{pillars.map((pillar,index)=>{const Icon=pillar.icon; return <article key={pillar.id} id={pillar.id} className="scroll-mt-28 grid gap-8 border-t border-border pt-10 lg:grid-cols-[.75fr_1.25fr]"><div><div className="flex size-14 items-center justify-center rounded-full bg-primary text-primary-foreground"><Icon className="size-6"/></div><p className="mt-6 text-xs font-extrabold text-accent">0{index+1}</p><h2 className="mt-2 text-3xl font-bold">{pillar.name}</h2><p className="mt-2 font-semibold text-primary">{pillar.category}</p><p className="mt-5 text-muted-foreground">{pillar.description}</p></div><div className="grid content-start gap-3 sm:grid-cols-2">{pillar.services.map(service=><div key={service} className="flex gap-3 border-b border-border py-3 text-sm"><Check className="size-4 shrink-0 text-accent"/>{service}</div>)}<Button asChild size="lg" className="mt-5 sm:w-fit"><Link to="/book">Book support <ArrowRight/></Link></Button></div><div className="form-shell lg:col-span-2"><h3 className="mb-6 text-xl font-bold">Enquire about {pillar.name}</h3><PillarForm pillarId={pillar.id} pillarName={pillar.name}/></div></article>})}</div></section></>}
