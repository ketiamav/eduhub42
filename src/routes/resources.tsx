import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";
import { articles } from "@/lib/site-data";

export const Route = createFileRoute("/resources")({ head: () => ({ meta: [{ title: "Education Resources & Guidance | EduHub42" }, { name: "description", content: "Practical EduHub42 resources for study skills, applications, education guidance and career readiness." }, { property: "og:title", content: "Education Resources & Guidance | EduHub42" }, { property: "og:description", content: "Knowledge and practical guidance for every stage of the learner journey." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: "/resources" }] }), component: ResourcesPage });
function ResourcesPage(){return <><section className="dark-band pt-44 pb-24"><div className="page-container"><p className="eyebrow">Knowledge for the journey</p><h1 className="section-title mt-4">Resources</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-primary-foreground/75">Practical guidance for learning well, accessing opportunities and preparing for what comes next.</p></div></section><section className="section-pad"><div className="page-container grid gap-x-8 md:grid-cols-2 lg:grid-cols-3">{articles.map(article=><article key={article.slug} className="article-card"><span className="eyebrow">{article.category}</span><h2 className="mt-4 text-2xl font-bold leading-8">{article.title}</h2><p className="mt-4 flex-1 text-sm leading-6 text-muted-foreground">{article.excerpt}</p><p className="mt-6 text-xs text-muted-foreground">{article.author} · {article.date}</p><Link to="/resources/$slug" params={{slug:article.slug}} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-primary">Read article <ArrowRight className="size-4"/></Link></article>)}</div></section></>}
